#!bin/bash

##    --  ln [OPTION]... [-T] TARGET LINK_NAME --   ##

  # * Crear links entre archivos ( como un acceso directo ) * #
  #

ln

  # El nombre del archivo no pertenece realmente al archivo;
    pertenece a una entrada de directorio que apunta al archivo.     

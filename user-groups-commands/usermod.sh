#!/bin/bash

##      --  usermod [options] LOGIN --     ##
    
    # usermod cambia la configuracion de un usario existente,
    # cuenta con las mimsa opciones que useradd, mas unas propias

# cambiar grupo de un usario

usermod -g dogtora teresa

    # -g -> reasignar el primary group de un usario
  

!/bin/bash

##      --  which [-as] filename ... --     ##
    
    # Muestra donde esta gaurdado el ejecutable que
    # que se paso como argumento
    #
    # which solo busca y muestra la locacion de los comandos
    # cuyos bins esten incluidos en la variable PATH

which note-template.sh 

    #  	
